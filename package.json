{
  "name": "testpack-cli",
  "version": "1.0.3",
  "description": "Verifies that your package will work after it is published by running your unit tests in a special test folder.",
  "main": "dist/testpack-cli.js",
  "scripts": {
    "test": "jest",
    "prepare": "tsc",
    "testpack": "node dist/index.js --verbose",
    "safePublish": "npm run testpack && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/qwertie/testpack.git"
  },
  "keywords": [
    "npm",
    "pack",
    "prepublish",
    "publish",
    "test",
    "check",
    "verify"
  ],
  "author": "David Piepgrass",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/qwertie/testpack/issues"
  },
  "homepage": "https://github.com/qwertie/testpack#readme",
  "dependencies": {
    "fs-extra": "^6.0.0",
    "glob": "^7.0.0",
    "minimist": "^1.2.0",
    "underscore": "^1.8.8"
  },
  "devDependencies": {
    "@types/fs-extra": "^5.0.4",
    "@types/glob": "^5.0.35",
    "@types/jest": "^23.1.5",
    "@types/minimist": "^1.2.0",
    "@types/node": "^10.5.2",
    "@types/tape": "^4.2.32",
    "@types/underscore": "^1.8.8",
    "jest": "^23.4.0",
    "ts-jest": "^23.0.0",
    "ts-node": "^7.0.0",
    "typescript": "^2.9.2"
  },
  "files": [
    "dist/*",
    "readme.md"
  ],
  "bin": {
    "testpack": "./dist/index.js",
    "testpack-cli": "./dist/index.js"
  },
  "jest-comment": [
    "We must include js in moduleFileExtensions, otherwise the following error happens:",
    "  Cannot find module './lib/source-map-generator' from 'source-map.js'",
    "However we only want to run ts test files, not js files generated by tsc.",
    "The solution I chose: put tests in their own folder and exclude it in tsconfig.json.",
    "Fun fact: I can't put this comment directly inside 'jest' or Jest will complain."
  ],
  "jest": {
    "transform": {
      "^.+\\.tsx?$": "ts-jest"
    },
    "testRegex": "(/tests/.*|(\\.|/)(test|spec))\\.(jsx?|tsx?)$",
    "testPathIgnorePatterns": [
      "<rootDir>.*Dress rehearsal.*"
    ],
    "moduleFileExtensions": [
      "ts",
      "tsx",
      "js",
      "jsx",
      "json"
    ],
    "bail": true
  },
  "testpack": {
    "test-folder": "Dress rehearsal",
    "clean": true,
    "regex": "ts,js#insideTestFolder = '(./yes)'#",
    "install": ["ts-jest", "babel-core", "typescript"],
    "packagejson": {
      "scripts": { 
        "fix-ts-jest": "mkdir src && echo \"//required for ts-jest to workaround issue #618\" > src/dummy.ts || cd .",
        "test": "npm run fix-ts-jest && jest && testpack --show-json"
      }
    }
  }
}
